[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Seed JSON + any committed public assets for serverless functions
[functions]
  included_files = ["data/*.json", "public/uploads/**"]

[[plugins]]
  package = "@netlify/plugin-nextjs"
